import DashboardClient from "./DashboardClient";

export const metadata = {
  title: "Client Dashboard | Eshaare Tours",
  description: "Manage your bookings, travel documents, visas, and profile settings in one secure place.",
};

export default function DashboardPage() {
  return <DashboardClient />;
}
